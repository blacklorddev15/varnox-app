# The dependency tree has an unresolvable peer conflict (@hookform/resolvers vs react-hook-form),
# so a plain `npm install` fails with ERESOLVE. Vercel runs npm install during the build, so the
# flag has to live in the repo rather than being passed on the command line.
legacy-peer-deps=true
